## readme for adding printing field functions:

# some rules:
	1. add new print function into a new folder which exactly the
           same name of the function.
        2. add common objects like structures and variables at
           'pr_for_fields_structs.h'

